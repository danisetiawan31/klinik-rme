# Done Log — Modul RME & Antrian Klinik

## Scaffolding Backend — Tahap 1: Foundation

- **Tanggal**: 2026-08-09
- **Apa yang Dikerjakan**:
  - Inisialisasi Go module `github.com/danisetiawan31/klinik-rme`.
  - Struktur folder standar Go: `cmd/server/`, `internal/config/`, `internal/db/`.
  - Config loader `internal/config/config.go` dengan validasi ketat env vars (`DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`, `TZ`). Error eksplisit jika env wajib kosong, tanpa *silent default*. `time.Local` set ke `TZ`.
  - Setup pgx pool (`pgx/v5`) di `internal/db/db.go`.
  - Graceful shutdown (`SIGINT`/`SIGTERM`) di `cmd/server/main.go`.
- **File Berubah/Dibuat**:
  - `go.mod`, `go.sum`
  - `internal/config/config.go`, `internal/config/config_test.go`
  - `internal/db/db.go`
  - `cmd/server/main.go`
- **Cara Verifikasi**:
  - `go test -v ./internal/config/...`
  - Run `./cmd/server` tanpa env -> Fatal error `missing required environment variable`.
- **Catatan**: Tidak ada endpoint HTTP & Gin di tahap ini.

---

## Scaffolding Backend — Tahap 2: Data Layer Tooling

- **Tanggal**: 2026-08-09
- **Apa yang Dikerjakan**:
  - Konfigurasi `sqlc.yaml` v2 (engine `postgresql`, schema `migrations`, queries `queries`, output `internal/db/generated/`, driver `pgx/v5`).
  - Dibuat folder `migrations/` & `queries/` (bersih hanya `.gitkeep`, tanpa file migrasi domain).
  - Wrapper library `golang-migrate/migrate/v4` di `internal/db/migration.go` dengan URI `file://` cross-platform (`filepath.ToSlash`), pre-check direktori & file `*.sql`, serta penanganan skip aman saat 0 file `.sql`.
  - Auto-run migrasi saat startup server di `cmd/server/main.go` sebelum DB pool dipakai.
  - Integration test & regresi test `internal/db/migration_test.go` menggunakan `testcontainers-go` lawan PostgreSQL asli (`postgres:16-alpine`).
- **File Berubah/Dibuat**:
  - `sqlc.yaml`
  - `migrations/.gitkeep`, `queries/.gitkeep`
  - `internal/db/migration.go`, `internal/db/migration_test.go`
  - `cmd/server/main.go`
- **Cara Verifikasi**:
  - `go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.29.0 generate` -> Exit code 0.
  - `go test -v ./internal/db/...` -> Test `TestRunMigrations_NonExistentPath`, `TestRunMigrations_EmptyFolder`, dan `TestRunMigrations_RealPostgreSQL` PASS 100%.
  - `go vet ./...` -> PASS.
- **Catatan**:
  - Pre-check direktori dan pencarian file `*.sql` dilakukan sebelum memanggil `golang-migrate` untuk mencegah error swallow pada path yang salah.
  - Header comment `// Code generated by sqlc. DO NOT EDIT.` pada hasil generator sqlc terverifikasi.

---

## Scaffolding Backend — Tahap 3: HTTP Layer

- **Tanggal**: 2026-08-09
- **Apa yang Dikerjakan**:
  - Setup Gin skeleton di `internal/api/router.go` & `cmd/server/main.go`.
  - Middleware `RequestID()` (`internal/api/middleware/request_id.go`): UUID v4 per request di-attach ke context & response header `X-Request-ID`.
  - Middleware `ErrorHandler` & `GlobalRecovery()` (`internal/api/middleware/error_handler.go`): Terstruktur `{ "error": { "code", "message", "requestId" } }`, `message` terkurasi aman, log raw error/stack trace di server-side.
  - Endpoint `GET /health` (`internal/api/handler/health.go`): `200` (`{"status":"ok","db":"ok"}`) jika DB reachable; `503` (`DB_UNAVAILABLE`) dengan `context.WithTimeout(c.Request.Context(), 2*time.Second)` jika DB down/menggantung.
  - Graceful shutdown HTTP server (timeout 5 detik) di `cmd/server/main.go`.
  - Unit & integration tests `error_handler_test.go` dan `health_test.go` (dengan `testcontainers-go`).
- **File Berubah/Dibuat**:
  - `internal/config/config.go`
  - `internal/api/middleware/request_id.go`, `internal/api/middleware/error_handler.go`, `internal/api/middleware/error_handler_test.go`
  - `internal/api/handler/health.go`, `internal/api/handler/health_test.go`
  - `internal/api/router.go`
  - `cmd/server/main.go`
- **Cara Verifikasi**:
  - `go test -v ./internal/api/...` -> PASS 100%.
  - `go vet ./...` -> PASS.
- **Catatan**:
  - `context.WithTimeout(c.Request.Context(), 2*time.Second)` dipasang di handler `GET /health` agar fail-fast saat DB mengalami retry/koneksi bermasalah.
