version: "2"
sql:
  - engine: "postgresql"
    schema: "migrations/*.up.sql"
    queries: "queries"
    gen:
      go:
        package: "dbgen"
        out: "internal/db/generated"
        sql_package: "pgx/v5"
